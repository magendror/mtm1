cmake_minimum_required(VERSION 3.0.0)
project(pq VERSION 0.1.0 LANGUAGES C)
set(MTM_FLAGS_DEBUG "-std=c99 --pedantic-errors -Wall -Werror -L. -lpriority_queue")
set(MTM_FLAGS_RELEASE "$(MTM_FLAGS_DEBUG) -DNDEBUG")
set(CMAKE_C_FLAGS ${MTM_FLAGS_DEBUG})
link_directories(.)
add_executable(pq_tests1 pq_tests1.c)
target_link_libraries(pq_tests1 libpriority_queue.a)